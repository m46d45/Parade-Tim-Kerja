# Parade Tim Kerja — Web (JS)

Simulasi **di browser** (migrasi dari Streamlit). Nama produk tetap.

```bash
cd web
npm install
npm test
npm run dev
```

Lihat [docs/MIGRATION-JS.md](../docs/MIGRATION-JS.md).
